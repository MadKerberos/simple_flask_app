version: '3.7'

services:

  server:
    build:
      context: .
      dockerfile: ./docker/dev/Dockerfile
    volumes:
      - ./:/app
    ports:
      - 5000:5000
    #env_file:
    #  - ./env/dev/.env